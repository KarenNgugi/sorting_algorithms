#include "sort.h"

/**
 * counting_sort - sorts in ascending order using counting sort algorithm
 * @array: array to sort
 * @size: size of array
 * Return: nothing
 */
void counting-sort(int *array, size_t size)
{
	return;
}
