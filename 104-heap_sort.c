#include "sort.h"

/**
 * heap_sort - sorts array using sift-down heap sort algorithm
 * @array: array to sort
 * @size: array size
 * Return: nothing
 */
void heap_sort(int *array, size_t size)
{
	return;
}
